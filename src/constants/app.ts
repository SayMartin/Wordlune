/**
 * User-facing product name, interpolated into i18n strings as {{app}} so the
 * translations never hardcode it. Not the same thing as the internal repo /
 * package name (`WordluneNative`) or the bundle id (`se.wordlune.app`).
 */
export const APP_NAME = "Wordlune";

/**
 * Copyright holder and publisher site shown in the web footer. A personal name
 * is not translated, so these stay out of the i18n files rather than becoming
 * three identical strings that could drift apart.
 */
export const OWNER_NAME = "Martin Persson";
export const OWNER_SITE_URL = "https://appfinningar.se";
export const OWNER_SITE_LABEL = "appfinningar.se";
