/**
 * User-facing product name, interpolated into i18n strings as {{app}} so the
 * translations never hardcode it. Not the same thing as the internal repo /
 * package name (`WordluneNative`) or the bundle id (`se.wordlune.app`).
 */
export const APP_NAME = "Wordlune";
